/*
split the string into an array
reverse the array
join the array into a string
*/

function string_reverse(string) {
  return string.split('').reverse().join('');
}

/*
loop through the string


*/

console.log(string_reverse('pants'));